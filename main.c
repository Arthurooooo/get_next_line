get_next_line

static char *data;
char *buf;

while read -> buf
data -> ft_strjoin(data, buf);
if ft_strchr(buf, '\n')
	break ;

*line = get_first_part(data); (return debut jusque \n)
data = get_second_part(data);
